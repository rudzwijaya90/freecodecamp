// dumb me, just use freecodecamp solution
function booWho(bool) {
  return typeof bool === "boolean";
}
  
  /* function booWho(bool) {
  if (bool === true){
    return true;
  } else if(bool === false){
    return true;
  } return false;
} */
booWho(null);

